# _archive

Files kept for reference. None of them are used by the running application.

| Path | Why it is here |
|---|---|
| `legacy_scripts/extract.py` | One-shot: pulled source files out of the original build guide. **Do not run.** |
| `legacy_scripts/patch.py` | One-shot rewriter of `pipeline.py`. It once stubbed the Santali TTS to silence. **Do not run.** |
| `legacy_scripts/optimize_models.py` | Warmed up the retired Hindi→English→Santali pivot models. |
| `legacy_scripts/run_setup.ps1` | Old setup script for the retired model set. |
| `VaaniSetu_WINNER_Build_Guide.md` | Original build guide. Predates the current architecture. |
| `frontend_drafts/frontend_updated.html` | CSS-only polish pass, since merged into `frontend.html`. Stale: lacks the Hindi-audio fix. |

Untracked scratch (old frontend drafts, `.bak` backups, test audio, sample PDFs) is kept here on disk only and is git-ignored.
