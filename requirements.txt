# Runtime dependencies for the laptop hub. Python 3.10 or 3.11, plus ffmpeg on PATH.
# Install with:  pip install -r requirements.txt
# torch CPU wheels are served from the PyTorch index.
--extra-index-url https://download.pytorch.org/whl/cpu

# Web server
Flask==3.1.3
flask-cors==6.0.5

# Speech recognition: IndicConformer 600M, ONNX
onnxruntime==1.29.0
soundfile==0.14.0
numpy==1.26.4

# Translation: IndicTrans2 indic-indic-dist-320M
torch==2.2.0+cpu
transformers==4.46.1
tokenizers==0.20.3
huggingface_hub==0.36.2
safetensors==0.8.0
sentencepiece==0.2.2
IndicTransToolkit @ git+https://github.com/VarunGumma/IndicTransToolkit.git@3efb8418d0721b4ce267c2b3586899d313191357

# Speech synthesis: Piper, fully offline
piper-tts==1.8.0

# Worksheets
reportlab==5.0.1

# Hub mode over HTTPS (tools/make_cert.py): a browser only allows the
# microphone on https:// or localhost, so a tablet on the Wi-Fi needs https.
cryptography==50.0.1

# Not listed on purpose:
#   gTTS    online Google TTS. Only used when config.ALLOW_ONLINE_TTS = True.
#   whisper retired ASR. Needed only if the IndicConformer files are missing.
